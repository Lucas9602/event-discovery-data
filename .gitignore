# Superpowers scratch (task briefs, review diffs, fix reports — ephemeral, survives compaction but not meant for history)
.superpowers/

# Local test fixture — fake events for browser testing, not real scraped data
app/public/events.json
