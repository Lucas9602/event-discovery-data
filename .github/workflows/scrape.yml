name: Scrape events

on:
  schedule:
    - cron: "0 3 */3 * *"
  workflow_dispatch: {}

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: scraper/package-lock.json

      - name: Install dependencies
        working-directory: scraper
        run: npm ci

      - name: Run scraper
        working-directory: scraper
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: npm run scrape

      - name: Run tests
        working-directory: scraper
        run: npm test

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/events.json data/health.json
          git diff --cached --quiet || git commit -m "Update scraped events [skip ci]"
          git push

  deploy-pages:
    needs: scrape
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      # No ref: here would default to github.sha — the commit that *triggered*
      # the run, frozen before the scrape job's own "Update scraped data"
      # commit. Check out the branch name instead so this job picks up that
      # commit, which is guaranteed to exist by the time this job starts
      # (needs: scrape).
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: data
      - uses: actions/deploy-pages@v4
        id: deployment
